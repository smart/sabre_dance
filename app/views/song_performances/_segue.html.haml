= link_to_remote "#{song_performance.segue? ? ">" : ","}", :update => "song_performance_#{song_performance.id}_segue", :url => toggle_segue_song_performance_url(song_performance), :html => {:class => "song_performance_segue_link  #{song_performance.segue? ? "segue" : "stop"}"}