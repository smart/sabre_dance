<h1>Listing show_set_lists</h1>

<table>
  <tr>
  </tr>

<% @show_set_lists.each do |show_set_list| %>
  <tr>
    <td><%= link_to 'Show', show_set_list %></td>
    <td><%= link_to 'Edit', edit_show_set_list_path(show_set_list) %></td>
    <td><%= link_to 'Destroy', show_set_list, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New show_set_list', new_show_set_list_path %>