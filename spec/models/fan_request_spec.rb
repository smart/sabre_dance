require 'spec_helper'

describe FanRequest do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    FanRequest.create!(@valid_attributes)
  end
end
